<!DOCTYPE HTML>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- SEO Meta Tags -->
  
<title>CipherGhostss | Admin Dashboard</title>
  <meta name="description" content="Explore chilling stories behind the screen with CipherGhostss — a blog uncovering cyber crimes, tech careers, and tales from the digital underworld.">
  <meta name="keywords" content="cybersecurity, cyber crimes, hacker stories, tech careers, digital forensics, cyber heists, cyberpunk blog, CipherGhostss, tech tales">
  <meta name="author" content="CipherGhostss">
  <meta name="robots" content="index, follow">

  <!-- Open Graph for Social Sharing -->
  <meta property="og:title" content="CipherGhostss | Cyber Crimes & Tech Tales">
  <meta property="og:description" content="Unveiling cyber heists and tech stories that live behind the screen. Discover how code, careers, and chaos shape our digital world.">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://www.cipherghostss.netlify.app/">
  <meta property="og:image" content="https://www.cipherghostss.netlify.app/assets/images/og-image.jpg">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="CipherGhostss | Cyber Crimes & Tech Careers">
  <meta name="twitter:description" content="Dive into digital shadows. Stories of hacks, heists, and hacker life.">
  <meta name="twitter:image" content="https://www.cipherghostss.netlify.app/assets/images/preview.jpg">

  <!-- Favicon -->
  <link rel="icon" href="/assets/images/cipher3.jpg" type="image/png">

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #111;
      color: #eee;
      padding: 2rem;
    }
    h1, h2 {
      color: #00ffff;
    }
    form {
      margin-bottom: 2rem;
      background: #222;
      padding: 1rem;
      border-radius: 8px;
    }
    input[type="text"], textarea {
      width: 100%;
      padding: 0.75rem;
      margin-bottom: 1rem;
      font-size: 1.1rem;
      border: none;
      border-radius: 4px;
      background: #333;
      color: #fff;
    }
    button {
      padding: 0.6rem 1.2rem;
      background: #00ffff;
      color: #000;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    ul {
      list-style: none;
      padding-left: 0;
    }
    li {
      background: #222;
      margin: 0.5rem 0;
      padding: 1rem;
      border-radius: 6px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    form.inline {
      display: inline;
    }
  </style>
</head>
<body>
  <h1>Admin Dashboard</h1>
  

  <form method="POST" action="/admin/create" onsubmit="return confirm('Create this new post?');">

    <input type="text" name="title" placeholder="Post Title" required />
    <textarea name="content" rows="8" placeholder="Write your post..." required></textarea>
    <button type="submit">Create Post</button>
    
  </form>

  <h2>All Posts</h2>
  <ul>
    <% posts.forEach(post => { %>
      <li>
        <span><strong><%= post.title %></strong></span>
        <div>
          <form method="POST" action="/admin/delete/<%= post._id %>" class="inline" onsubmit="return confirm('Are you sure you want to delete this post?');">
          <button type="submit">Delete</button>
        </form>

        <form method="GET" action="/admin/edit/<%= post._id %>" class="inline" onsubmit="return confirm('Do you want to edit this post?');">
          <button type="submit">Edit</button>
        </form>
        </div>
      </li>
    <% }) %>
  </ul>
</body>
</html>
