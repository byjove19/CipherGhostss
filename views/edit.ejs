<!DOCTYPE html>
<html>
<head>
  <title>Edit Post - CipherGhostss</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #111;
      color: #eee;
      padding: 2rem;
    }
    h1 {
      color: #00ffff;
    }
    form {
      background: #222;
      padding: 1.5rem;
      border-radius: 8px;
    }
    input[type="text"], textarea {
      width: 100%;
      padding: 0.75rem;
      margin-bottom: 1rem;
      font-size: 1.1rem;
      border: none;
      border-radius: 4px;
      background: #333;
      color: #fff;
    }
    button {
      padding: 0.6rem 1.2rem;
      background: #00ffff;
      color: #000;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Edit Post</h1>
  <form method="POST" action="/admin/edit/<%= post._id %>">
    <input type="text" name="title" value="<%= post.title %>" required />
    <textarea name="content" rows="10" required><%= post.content %></textarea>
    <button type="submit">Update</button>
  </form>
</body>
</html>
