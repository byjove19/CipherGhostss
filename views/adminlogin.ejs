<!DOCTYPE html>
<html>
<head>
  <title>Admin Login - CipherGhostss</title>
  <style>
    body {
      background: #111;
      color: #eee;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    form {
      background: #222;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 0 10px #00ffff;
    }
    input {
      width: 100%;
      margin: 0.5rem 0;
      padding: 0.8rem;
      background: #333;
      color: #fff;
      border: none;
      border-radius: 4px;
    }
    button {
      background: #00ffff;
      color: #000;
      padding: 0.8rem 1.2rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-top: 1rem;
    }
  </style>
</head>
<body>
  <form method="POST" action="/adminlogin">
    <h2>Admin Login</h2>
    <input type="text" name="username" placeholder="Username" required />
    <input type="password" name="password" placeholder="Password" required />
    <button type="submit">Login</button>
  </form>
</body>
</html>
